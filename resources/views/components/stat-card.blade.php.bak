@props(['icon', 'label', 'value'])
<div class="stat-card">
    <div class="stat-icon"><i class="fa-solid {{ $icon }}"></i></div>
    <div>
        <span>{{ $label }}</span>
        <strong>{{ $value }}</strong>
    </div>
</div>
